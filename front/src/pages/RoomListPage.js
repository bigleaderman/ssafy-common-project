import React from "react";
import { Container } from '../style.js';


const RoomListPage = (props) => {
  return (
    <Container>
      <span>RoomListPage</span>
    </Container>
  );
};

export default RoomListPage;